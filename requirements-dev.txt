# Lambda dependencies (for local testing)
bcrypt==4.1.2

# AWS SDK (already in Lambda, but needed locally)
boto3==1.34.0
botocore==1.34.0

# Testing frameworks
pytest==7.4.3
pytest-cov==4.1.0
pytest-mock==3.12.0
hypothesis==6.92.1

# AWS mocking
moto==4.2.0

# Development tools (optional)
black==23.12.0
mypy==1.7.0